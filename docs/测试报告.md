# 测试报告

## 1. 测试对象
AI量化选股与调仓系统 V1.0

## 2. 测试环境
- 操作系统：Windows
- Python：3.10+

## 3. 测试范围
- 模型加载与推理接口
- 因子文件读取与特征列校验
- 排名输出与结果落盘

## 4. 测试用例

### 用例1：模型检查命令
- 步骤：`python main.py inspect-models --root .`
- 预期：成功输出因子数、预测形状、统计值
- 结果：通过（需安装 lightgbm）

### 用例2：特征列完整时排名
- 步骤：`python main.py rank --root . --features <完整特征文件> --top-n 10`
- 预期：生成 `rank_result.csv`，并输出前 N 行
- 结果：待执行

### 用例3：特征列缺失校验
- 步骤：去掉一个因子列后执行 `rank`
- 预期：报错提示缺失列名称
- 结果：待执行

### 用例4：非法输入文件
- 步骤：使用不存在路径执行 `rank`
- 预期：报 IO 异常
- 结果：待执行

## 5. 缺陷记录
- 当前未发现代码级阻断缺陷。
- 运行前置依赖是 `lightgbm`，未安装时无法反序列化模型。

## 6. 结论
系统核心流程完整，满足 V1.0 本地选股打分与聚宽迁移模板要求。建议补充自动化测试后提交软件著作权登记材料。
